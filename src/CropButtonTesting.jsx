import React from 'react';
import AssetButton from "../demos/AssetButton"

const CropButtonTesting = () => {
    return (
        <AssetButton asset="CropSong.svg" hintTrueString=" ...tap & drag to crop the song:"/>
    );
}

export default CropButtonTesting;

